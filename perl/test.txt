class User {
	function foo() :db {
		for(int $i;$i <= 10;$i++) {
			4;
		}
	}
}
function a(){}
__END
class User :primary(id) {
	int    id :auto_increment;
	string username;
	string email;
	int    status;
	
	function getByStatus(int $status = 0) :db {
		string filter :foo = "hi";
		return 
			from(User)
				->read(id, username)
				->where(status == $status);
	}
	
	function getUserMeta(int $id) :db {
		return
			from(User)
				->read(id, username, email, status)
				->where(id == $id)
			->innerjoin(UserMeta)
				->read(metadata)
				->where(id == $id);
	}
}

class UserMeta :primary(id) {
	int id :auto_increment;
	string metadata :match('/[a-z]+/');
}
