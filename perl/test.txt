
class User :primary(id){
	int id :auto_increment;
	string username;
	int    nation;
	int    active;
	
	dbfunction getByActive(int active :default(0)) {
		string f :filter(User.active.foo.bar);
		read('*')->where(f);
	}
}
