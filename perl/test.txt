
class User :primary(id){
	int id :auto_increment;
	string username;
	int    nation;
	int    active;
	
	dbfunction getByActive(int $active =0) {
		return 
			from(User)
			->read(id, username)
			->where(active == $active);
	}
}
