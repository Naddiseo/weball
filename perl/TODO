- make parser allow for black lines in a block
- add more keyword/functions for dbfunctions
	dbfunction foo(Foo.a, Bar.b)
		# delete from Foo where Foo.id=a;
		delete(Foo,Foo.id,a)
		# update Bar SET Bar.a=b, Bar.c=1, where Bar.id=b
		update(Bar,Bar.id,b,Bar.a,b,Bar,c,1)

- add more complex dbfunctions, such as flow control
	dbfunction foo(Foo.a, Bar.b)
		int myvar default 0
		if(a == b)
			delete(Foo, Foo.id, a)
		elsif(a > b)
			delete(Bar, Bar.id, b)
- support compare ops in arg list
- support function properties? fnName(args).property(args).property(args)
- make dbfunction calling look nicer
	something like:
	delete(Foo).where(id = a).limit(1)
		
		
