typedef uint t_test :range(0, 5)
	
class Foo
	pk(id)
	t_test id :auto_increment
	uint fee
	bool fi
	string fo
	int fum
	dbfunction getFum(id, fi)
		return(fum)
		
class Shu
	pk(id)
	id t_test

include("./bar.all")		

dbfunction getTroll(Foo.id)
	return(Bar.troll)
	join(Foo.id, Bar.id, Shu.id)
	

config
	MYKEY("value")
	KEY2(42)
